name: saltproc-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - openmc>=0.14.0
  - numpy
  - pytables
  - networkx
  - pydot
  - pytest
  - jsonschema
  - pip:
    - git+https://github.com/CORE-GATECH-GROUP/serpent-tools.git
    - argparse
